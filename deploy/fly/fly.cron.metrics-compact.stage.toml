# fly.cron.metrics-compact.stage.toml - Daily Summer League metric-version compaction
#
# One-time staging setup:
#   IMAGE=$(flyctl machine list --app draft-app --json | jq -r '[.[] | select(.config.metadata.fly_process_group == "app")] | first | .config.image')
#   flyctl machine run "$IMAGE" \
#     --app draft-app \
#     --schedule daily \
#     --name summer-league-metrics-compact-cron \
#     --region ewr \
#     --memory 256 \
#     --cpus 1 \
#     --entrypoint "/app/.venv/bin/python" \
#     -- -m app.cli.summer_league_metrics_compact

app = 'draft-app'
primary_region = 'ewr'

[build]

[processes]
  cron = "/app/.venv/bin/python -m app.cli.summer_league_metrics_compact"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
