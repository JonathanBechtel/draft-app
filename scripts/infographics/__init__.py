"""Share infographics: turn live app data into self-contained, screenshot-ready HTML.

Pipeline (see ``render.py``): load data → embed player faces → render a template
→ (Phase 2) verify visually → screenshot for X/Twitter. Templates live as flat
modules (``scatter`` today; ``leaderboard``/``hero`` next) and share the visual
chrome in ``theme.py``.
"""
