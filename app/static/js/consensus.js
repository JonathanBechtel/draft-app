/* ==========================================================================
   consensus.js — Global page-level JS for /consensus
   Per-section JS lives in consensus-board.js, consensus-scatter.js,
   consensus-trajectories.js. Ticket #278 consolidates them all back here.
   ========================================================================== */

document.addEventListener("DOMContentLoaded", function () {
  // Global consensus page initialisation hook.
  // Section-specific modules initialise themselves via their own DOMContentLoaded
  // listeners. Place cross-section coordination logic here.
});
