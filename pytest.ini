[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    committed_db: use independent database connections and truncate isolation for concurrency or durability behavior
# Keep local runs serial and deterministic by default. CI explicitly uses the
# measured worker count for its four-core runner.
