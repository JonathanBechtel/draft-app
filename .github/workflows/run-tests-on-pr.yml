name: PR Tests

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: pr-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests-fork:
    # Safe path for PRs from forks: no secrets, ephemeral Postgres service
    if: ${{ github.event.pull_request.head.repo.fork == true }}
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: draftguru_test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      # App/test config
      ENV: stage
      DEBUG: "0"
      ACCESS_LOG: "0"
      SQL_ECHO: "0"
      AUTO_INIT_DB: "0"  # rely on Alembic + test fixtures
      SECRET_KEY: test
      # DB config (use service container)
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/draftguru_test
      TEST_DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/draftguru_test
      PYTEST_ALLOW_DB: "1"
      # CI runs against an ephemeral service container, so DATABASE_URL and
      # TEST_DATABASE_URL intentionally point at the same DB. Allow it.
      PYTEST_ALLOW_TEST_DB_EQUALS_DATABASE_URL: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Full history so diff-cover can compare against the PR base branch.
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: |
            pyproject.toml

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Lint (ruff)
        run: ruff check .

      - name: Cache mypy incremental state
        uses: actions/cache@v4
        with:
          path: .mypy_cache
          key: mypy-${{ runner.os }}-py312-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            mypy-${{ runner.os }}-py312-

      - name: Type check (mypy)
        run: mypy app --ignore-missing-imports

      - name: Reset test database (drop schema)
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
        run: |
          python - <<'PY'
          import os
          import psycopg

          raw_url = os.environ["DATABASE_URL"]
          url = raw_url.replace("+asyncpg", "")
          conn = psycopg.connect(url, autocommit=True)
          with conn.cursor() as cur:
              cur.execute("DROP SCHEMA IF EXISTS public CASCADE;")
              cur.execute("CREATE SCHEMA public;")
          conn.close()
          print("Reset schema public in test database")
          PY

      - name: Alembic upgrade head
        run: alembic upgrade heads

      - name: Run tests
        # -n 4 fans tests across the runner's 4 vCPUs; each xdist worker is its
        # own pytest session, so the integration fixtures give every worker an
        # isolated schema (see tests/integration/conftest.py test_schema).
        run: pytest -q --ignore=tests/visual -n 4 --cov=app --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          flags: tests-fork
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Patch coverage gate (diff-cover)
        # Fail the PR if fewer than 80% of newly changed app/ lines are covered.
        # Compares the working tree against the PR base branch (merge-base).
        run: diff-cover coverage.xml --compare-branch=origin/${{ github.base_ref }} --fail-under=80

  tests-secrets:
    # Same-repo PRs: use environment secrets from draft-app-stage
    if: ${{ github.event.pull_request.head.repo.fork == false }}
    runs-on: ubuntu-latest
    environment: draft-app-dev
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: draftguru_test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    env:
      ENV: dev
      DEBUG: "0"
      ACCESS_LOG: "0"
      SQL_ECHO: "0"
      AUTO_INIT_DB: "0"  # rely on Alembic + test fixtures
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/draftguru_test
      TEST_DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/draftguru_test
      PYTEST_ALLOW_DB: "1"
      # CI runs against an ephemeral service container, so DATABASE_URL and
      # TEST_DATABASE_URL intentionally point at the same DB. Allow it.
      PYTEST_ALLOW_TEST_DB_EQUALS_DATABASE_URL: "1"
      # SECRET_KEY may be provided by the environment; default to test if missing
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Full history so diff-cover can compare against the PR base branch.
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: |
            pyproject.toml

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Lint (ruff)
        run: ruff check .

      - name: Cache mypy incremental state
        uses: actions/cache@v4
        with:
          path: .mypy_cache
          key: mypy-${{ runner.os }}-py312-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            mypy-${{ runner.os }}-py312-

      - name: Type check (mypy)
        run: mypy app --ignore-missing-imports

      - name: Default SECRET_KEY when missing
        run: |
          if [ -z "${{ secrets.SECRET_KEY }}" ]; then
            echo "SECRET_KEY=test" >> $GITHUB_ENV
          fi

      - name: Reset test database (drop schema)
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
        run: |
          python - <<'PY'
          import os
          import psycopg

          raw_url = os.environ["DATABASE_URL"]
          url = raw_url.replace("+asyncpg", "")
          conn = psycopg.connect(url, autocommit=True)
          with conn.cursor() as cur:
              cur.execute("DROP SCHEMA IF EXISTS public CASCADE;")
              cur.execute("CREATE SCHEMA public;")
          conn.close()
          print("Reset schema public in test database")
          PY

      - name: Alembic upgrade head
        run: alembic upgrade heads

      - name: Run tests
        # -n 4 fans tests across the runner's 4 vCPUs; each xdist worker is its
        # own pytest session, so the integration fixtures give every worker an
        # isolated schema (see tests/integration/conftest.py test_schema).
        run: pytest -q --ignore=tests/visual -n 4 --cov=app --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          flags: tests-secrets
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Patch coverage gate (diff-cover)
        # Fail the PR if fewer than 80% of newly changed app/ lines are covered.
        # Compares the working tree against the PR base branch (merge-base).
        run: diff-cover coverage.xml --compare-branch=origin/${{ github.base_ref }} --fail-under=80
